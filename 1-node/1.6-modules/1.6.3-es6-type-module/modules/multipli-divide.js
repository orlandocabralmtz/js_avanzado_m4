function multiply(a, b) {
    return a * b;
}

function divide(a, b) {
    return a / b;
}

export {    // no se usa module export. Ya se usa directamente la palabra export
    multiply,
    divide
}
